<div class='container row'>
          <div class='container-fluid'>
            <h2 id="recipeTitle">{{Title}}</h2>
          </div>
            <div class='col-4 .col-lg-4'>
                <ul id="ingredient" >
                    {{ingredients}}
                </ul>
            </div>
            <div class='col-8 .col-lg-8 vertical'>
                <img id='fullrecipeimg' src='{{image}}'>
                <h5>Instructions:</h5>
                {{instructions}}
            </div>    
</div>